import random

n_runs = 10
n_pts = 10

for ( i in 1:n_runs ) :
    # choose 2 random numbers
    x_t = random.uniform ( -1, 1 )
    y_t = random.uniform ( -1, 1 )
    
    # get the classifier
    # create data

    # train the data using PLA
    
    # get the misc on a seperate set of points
    # store misc rate and no. of iterations
